{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Prepare logs_report Debug",
      "dependsOrder": "sequence",
      "dependsOn": [
        "Install log_processor debug dependencies"
      ],
      "problemMatcher": []
    },
    {
      "label": "Install log_processor debug dependencies",
      "type": "shell",
      "command": "python3.12 -m venv .venv && .venv/bin/python -m pip install --quiet --no-cache-dir -r lambdas/log_processor/requirements.txt \"pytest>=8,<9\"",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    },
    {
      "label": "Prepare cost_explorer Debug",
      "dependsOrder": "sequence",
      "dependsOn": [
        "Install cost_explorer debug dependencies"
      ],
      "problemMatcher": []
    },
    {
      "label": "Install cost_explorer debug dependencies",
      "type": "shell",
      "command": "python3.12 -m venv .venv && .venv/bin/python -m pip install --quiet --no-cache-dir -r lambdas/cost_explorer/requirements.txt",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}
