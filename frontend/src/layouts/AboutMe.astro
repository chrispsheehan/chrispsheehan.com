<section id="about-me" class="section-shell">
  <div class="section-heading" data-reveal>
    <p class="section-eyebrow">About me</p>
    <h2>Platform work grounded in delivery reality.</h2>
  </div>

  <div class="about-grid">
    <div class="about-copy" data-reveal>
      <h3>
        I'm Chris, a Senior Platform Engineer focused on developer experience,
        CI/CD, and cloud platform delivery.
      </h3>
      <p>
        I design AWS and Terraform-based platform foundations, delivery
        workflows, and self-service tooling that help engineering teams ship
        faster with less friction.
      </p>
      <p>
        My background in software quality engineering and SDET leadership
        shapes how I build: strong guardrails, practical automation, clear
        standards, and better release confidence.
      </p>
      <p>
        I also use AI-assisted workflows, including Codex and other LLM tools,
        to accelerate delivery, automation, and documentation without lowering
        the bar on quality.
      </p>
    </div>

    <div class="focus-panel" data-reveal>
      <div class="focus-card">
        <p class="section-eyebrow">Selected focus areas</p>
        <ul class="focus-list">
          <li>
            <strong>Developer Enablement</strong>
            <span>Self-service paths, safer defaults, and less release friction.</span>
          </li>
          <li>
            <strong>Delivery Systems</strong>
            <span>CI/CD pipelines that stay readable, testable, and dependable.</span>
          </li>
          <li>
            <strong>Cloud Foundations</strong>
            <span>AWS and Terraform patterns built for reuse rather than ceremony.</span>
          </li>
          <li>
            <strong>Operational Feedback</strong>
            <span>Observability and diagnostics that shorten the path to action.</span>
          </li>
        </ul>
      </div>
    </div>
  </div>
</section>

<style>
  .about-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.5fr) minmax(280px, 0.9fr);
    gap: 1.5rem;
    align-items: start;
  }

  .about-copy h3 {
    max-width: 18ch;
  }

  .focus-card {
    padding: 1.5rem;
    border-radius: 1.25rem;
    background: linear-gradient(
      180deg,
      rgba(16, 35, 28, 0.72) 0%,
      rgba(11, 25, 20, 0.9) 100%
    );
    border: 1px solid var(--color-border-soft);
    box-shadow: var(--shadow-card-hover);
  }

  .focus-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 1rem;
  }

  .focus-list li {
    display: grid;
    gap: 0.25rem;
  }

  .focus-list strong {
    color: var(--color-heading);
    font-size: 1rem;
  }

  .focus-list span {
    color: var(--color-text-soft);
  }

  @media (max-width: 900px) {
    .about-grid {
      grid-template-columns: 1fr;
    }

    .about-copy h3 {
      max-width: none;
    }
  }
</style>
