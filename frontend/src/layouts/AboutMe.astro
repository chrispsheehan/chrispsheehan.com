<section id="about-me" class="section-shell">
  <div class="section-heading" data-reveal>
    <p class="section-eyebrow">About</p>
    <h2>Platform engineering focused on delivery and clear operational ownership.</h2>
  </div>

  <div class="about-grid">
    <div class="about-copy" data-reveal>
      <h3>
        I build the systems behind software delivery: cloud foundations,
        deployment workflows, and the guardrails teams rely on every day.
      </h3>
      <p>
        My work centres on AWS, Terraform, CI/CD, and developer experience for
        teams that need speed without losing release confidence.
      </p>
      <p>
        A background in quality engineering shapes how I build: readable
        automation, safer defaults, and delivery paths that are easy to trust.
      </p>
    </div>

    <div class="focus-panel" data-reveal>
      <div class="focus-card">
        <p class="section-eyebrow">Current focus</p>
        <ul class="focus-list">
          <li>
            <strong>Platform Architecture</strong>
            <span>AWS and Terraform patterns built for reuse and clear ownership.</span>
          </li>
          <li>
            <strong>Release Engineering</strong>
            <span>CI/CD paths that stay dependable as teams and services grow.</span>
          </li>
          <li>
            <strong>Developer Enablement</strong>
            <span>Self-service workflows and guardrails that reduce support drag.</span>
          </li>
          <li>
            <strong>Operational Clarity</strong>
            <span>Runtime visibility that helps engineers act quickly when things move.</span>
          </li>
        </ul>
      </div>
    </div>
  </div>
</section>

<style>
  #about-me .section-heading h2,
  #about-me .about-copy h3 {
    max-width: none;
  }

  .about-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.5fr) minmax(280px, 0.9fr);
    gap: 1.5rem;
    align-items: start;
  }

  .focus-card {
    padding: 1.5rem;
    border-radius: 1.25rem;
    background: linear-gradient(
      180deg,
      rgba(16, 35, 28, 0.72) 0%,
      rgba(11, 25, 20, 0.9) 100%
    );
    border: 1px solid var(--color-border-soft);
    box-shadow: var(--shadow-card-hover);
  }

  .focus-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 1rem;
  }

  .focus-list li {
    display: grid;
    gap: 0.25rem;
  }

  .focus-list strong {
    color: var(--color-heading);
    font-size: 1rem;
  }

  .focus-list span {
    color: var(--color-text-soft);
  }

  @media (max-width: 900px) {
    .about-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
